services:
  - type: web
    name: jobinsight-api
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: EXTENSION_ID
        sync: false
      - key: PORT
        value: 3000
